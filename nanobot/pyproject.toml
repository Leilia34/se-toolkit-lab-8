[project]
name = "nanobot-project"
version = "0.1.0"
description = "Nanobot agent for LMS"
requires-python = ">=3.14"
dependencies = [
    "nanobot-ai>=0.1.4",
    "lms-mcp",
    "mcp-observability",
]

[tool.uv.sources]
lms-mcp = { workspace = true }
mcp-observability = { workspace = true }
